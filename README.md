# alexcatdad.github.io

Personal GitHub Pages site.

## Project metadata (`.project.json`)

Project card inclusion and rendering for `/projects` is driven by per-repo `/.project.json`.

- Spec and behavior: `docs/project-metadata.md`
- JSON Schema: `docs/project-meta.schema.json`
